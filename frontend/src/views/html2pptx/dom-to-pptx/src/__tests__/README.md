# Upstream tests placeholder

The upstream `src` tree includes `__tests__`. This route-local vendor copy keeps
the directory so future upstream source/test files can be dropped in without
changing import paths.
